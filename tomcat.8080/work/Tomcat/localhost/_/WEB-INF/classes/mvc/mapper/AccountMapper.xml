<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mvc.mapper.AccountMapper">
    <select id="queryAccountList" resultType="mvc.pojo.Account">
        select *
        from account_test
    </select>
</mapper>
